apiVersion: 1
datasources:
  - name: Jaeger
    type: jaeger
    uid: jaeger
    access: proxy
    # QUERY_BASE_PATH=/jaeger prefixes the query API too.
    url: http://jaeger:16686/jaeger
    editable: false
